import { AlertInputForm } from './AlertInputForm';

export interface ErrorsFormCategory {
    name?: AlertInputForm | null;
    slug?: AlertInputForm | null;
    description?: AlertInputForm | null;
    parent_id?: AlertInputForm | null;
    position?: AlertInputForm | null;
}
